Using Windows? Click Launch.cmd and have fun!
Other OS? It should still work but you need Python 3.7 and the requirements from requirements.txt.
A simple way of installing them is using the following command :  pip install -r requirements.txt